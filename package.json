{
  "name": "netlify-serverless-nodejs10",
  "version": "1.0.0",
  "description": "Using AWS's serverless Lambda functions with Netlify",
  "scripts": {
    "dev": "netlify-lambda serve lambda",
    "build": "netlify-lambda build lambda"
  },
  "dependencies": {
    "netlify-lambda": "^1.6.3"
  }
}
